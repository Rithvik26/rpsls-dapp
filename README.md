RPSLS - minimal dApp (Kleros exercise)
1) Deploy original RPS.sol using Remix (Injected Provider -> Sepolia).
2) Copy contract address and ABI to src/utils/contractABI.js
3) npm install
4) npm run dev
5) Connect MetaMask (Sepolia), test Create -> Join -> Reveal flows.
